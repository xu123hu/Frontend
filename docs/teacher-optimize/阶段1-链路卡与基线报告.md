# 阶段 1 交付：基线提交 + 链路卡全集 + 断头路清零方案 + 双叙事合流（2026-09-01）

> 上游：阶段 0 调研报告（已验收）。本文件为阶段 1 交付物，**提交用户验收，验收通过前不进入阶段 2，不动业务代码**（基线提交除外，系裁决④a 授权动作）。

---

## 1. 第一动作：在途改动分组提交（裁决④a 执行记录）

基线健康度（提交前实测）：
- **vitest：31 文件 / 200 用例全部通过**；
- **typecheck：2 处红** —— `test/student/dualClassroom.test.ts:39,69`（TS2339，属性不存在）。属上一轮学生端在途工作遗留，非本对话引入，已如实写入提交说明；不影响教师端链路验收。

| 提交 | 主题 | 内容 |
|---|---|---|
| `5717451` | feat(teacher-classroom) | 课堂 Session 重写：api/stores 四端点、视图 V3 重写、类型、契约测试（已知债写入提交说明） |
| `175fb7c` | feat(teacher-grading) | 批改 V2 批后讲评面板 + question.image 原题图（BE→FE shipped 项） |
| `2a609a8` | feat(student) | 试卷页 /exam/:id、/dual 沉浸式、3D 图形、tabular 渲染、双师工具链、学生课堂闭环 6 端点 |
| `1702284` | chore(core) | three 依赖、api.delete、vite host、登录角色修正 |
| `fd952d6` | docs | 阶段 0 调研报告 + 10 张竞品截图 |

提交后 `git status` 干净。与裁决分组口径的偏差：裁决列 4 组，实际拆出第 5 组"教师端批改 V2 讲评面板"——该组与课堂无关且可独立回滚，更符合"主题分组"本意。

**【BE 答复】来源核验**：我方仅写入 5 条 FE→BE 行；【BE 答复】与 6 条 BE→FE 行系其后出现于共享文件，含后端内部证据（`insights.py` 候选链、`teacher/router.py:199`、`Field(default=0,ge=0,le=99)`、`test_m3_teacher_classroom.py`），判定**确为后端对话所写**，且与后端仓库现状交叉一致（我方已实际读到 `D:\math-arena\services\api\tests\test_m3_teacher_classroom.py` 的断言，与答复吻合）。

## 2. 课堂在途改动专项评审（裁决④b）

亲读在途代码（TeacherClassroomView.vue 全文 294 行 + api/store diff）后逐项裁决：

| 裁决项 | 结论 | 证据 | 整改方案（归阶段 3 L6） |
|---|---|---|---|
| 硬编码三题 | **确认存在，且比断点更深**：前端卡片文案是自编剧面，后端实际按 `question_no % 题池长度` 出**自己的题**（test_m3_teacher_classroom.py:114-157 证实确定性出题）——师生两端看到的是两套题 | TeacherClassroomView.vue:149-154；后端断言 distribution 4 项确定性 | 过渡：卡片改中性标签"检测点 1/2/3"，发题后渲染后端返回的 `last_question.prompt`（视图 :84 已具备）；治本：契约新增"题池暴露"（已写契约记录，proposed） |
| 幂等回退 | **确认回退**：`client_request_id` 拼 `Date.now()`=每次重试新键；store 删除了 `createIdempotencyTracker` | api/teacher/classroom.ts:53 | 恢复 tracker，键改 `classroom:launch:{classId}:{questionNo}`（0 基已确认）；"再次发题"语义=新 round，由 question_no 区分，不需新键 |
| 死导入 | **确认**：`import { classroomApi }`（:124）全文未使用 | :124 | 删除 |
| mock 零支持 | **确认**：teacherServer.ts 零 session 路由；契约测试断言了不存在的 mock | §0 断点 #10 | mock 镜像后端测试断言：GET 无会话→`status:"idle"`；start→`active`；question_no 0 基→distribution 4 项确定性；close→`ended`；新增课堂 e2e |
| （亲读新增）"加入讲解（备课）"裸跳转 | 从课堂推题到备课**不带任何焦点上下文**，L6→L2 闭环断 | :210 `goPrep()` | 写 context store（focus/prompt/question_no），Prep 消费高亮目标环节 |
| （亲读新增）已进行分钟数不跳动 | `elapsedMinutes` 基于一次性 `Date.now()`，无定时刷新 | :156-160 | 30s 轮询或 interval 刷新（低优先） |
| （亲读新增）作答前分布占位 | 后端自述作答前分布为"待作答/合成"占位，阶段 3 诚实化 | 契约记录 BE 答复 | 前端按后端诚实化字段渲染；在诚实化落地前 UI 标注"模拟分布"（明示降级，禁静默） |

**评审总结论**：方向正确（Session 语义对齐 GP-13、对标 SchoolAI Mission Control），**保留为基线**；上述 7 项整改全部挂 L6 链路卡，阶段 3 按卡执行。

## 3. 教师页双解包排查（裁决⑤升必做，已关闭）

- **结论：教师端零命中**。全量 grep `src/pages/teacher*`、`src/stores/teacher`、`src/components/teacher`、`src/features/teacher-grading-v2`，`\.code !== 0`、`res?.code`、`.data.data` 等模式 **0 处命中**。
- 机制证据：`api/client.js:102-104` 信封统一解包（code!==0 抛 ApiError）；教师侧统一经 `api/teacher/client.ts` 走 `api.raw` 取 `{data,status,request_id}`——学生端的事故模式（页面再判 `res.code`）在教师端不存在结构性入口。
- 残留相关风险：4 处裸 fetch 绕过 client（PrepView:405/:421、ResourcesView:124、gradingWorkspace.ts:33，即 N3）——不是双解包，归 P1-5 收敛，不阻塞本项关闭。

## 4. 双叙事合流方案（附加要求 b）

两套叙事的实测关系：teacher-v2 的重建方法**事实上已应用于原路由页面内部**（Assign/Classes/Profile/Resources 四页 V2 参照头注释都写在 `pages/teacher/` 原文件里），只有 Grading 产生了独立目录。合流是"追认现实 + 补齐治理"，不是新工程：

| 维度 | 合流裁决（待确认） |
|---|---|
| 产品 SSOT | 维持 teacher-refactor 治理文档：PRODUCT_VISION / BUSINESS_OBJECTS（统一数据世界）/ ACCEPTANCE（10 条 UX Gate + 红线 R1-R5）/ IA（导航与 URL 寻址 FROZEN）继续有效 |
| 施工方法论 | 采用 teacher-v2 方法：One Module One Primary Reference（reference-map.md 为唯一参照登记处）、clone+license+trace 三件套、Reference/Legacy/V2 三方硬门截图 |
| 路由 | 8 条路由不动（IA FROZEN）；模块升级只改 `pages/teacher/` 原文件；`pages/teacher-v2/` 冻结为 Grading 专属，不再新增平级目录 |
| 验收 | 唯一验收脚本 = GOLDEN_PATH GP-1..14 × 本期链路卡 L1-L8 对账（§5 映射表）；e2e 为 GP 步骤的自动化锚点；UX Gate/红线作为卡内约束逐条挂 |
| 指令目录 | `docs/teacher-optimize/` 为本期唯一活动指令目录；teacher-refactor/CURRENT_DIRECTIVE（R06）**冻结存档**，其未完成项按下表并入本期 |
| Legacy 处置 | "Legacy 永不编辑"铁律转为：V1 批改孤儿页按资产表 #8 收敛删除（V2 承接），删除动作本身走验收用例 |

**R06 未完成项去向**：Phase A（saveDraft 归一 segments 单轨）→ 阶段 3 L2；Phase B（编辑持久化 PUT artifacts——mock 已有 409 乐观锁，视图接线待复核）→ 阶段 3 L2；Phase C（闭环右半环 GP-11..14）→ 即本期黄金路径主攻方向。

## 5. GOLDEN_PATH GP-1..14 ↔ 链路卡映射对账（附加要求 b）

| GP | 内容 | 链路卡 | 卡内步骤 | 现状（核验） | 挂钩 |
|---|---|---|---|---|---|
| GP-1 | 登录→Today hero/任务/洞察 | L1 | S1-S4 | 可用；T-01 问候语、:179 硬编码待清 | L1 用例 |
| GP-2 | 洞察【加入下节课】→备课 diff | L1→L2 | L1.S4→L2.S3 | 跳转真实；diff 高亮待运行时复核 | L2 用例 |
| GP-3 | 采纳→时间线真实变化 | L2 | S4 | **断**（仅本地态，无落库） | P0-3（契约已 accepted） |
| GP-4 | 结构化编辑（禁 prompt） | L2 | S5 | **断**（window.prompt×3，GP FAIL 条件命中） | P0-3 |
| GP-5 | 确认本节课（状态可见） | L2 | S6 | 可用（artifacts confirm+409 乐观锁） | L2 用例 |
| GP-6 | 生成当堂检测→蓝图预填 | L2→L3 | L2.S7→L3.S2 | **断**（Assign 不消费课题/知识点上下文，亲读核实） | L2→L3 断头路 #7 |
| GP-7 | 逐题换题（仅该题变） | L3 | S4 | **断**（replace/regenerate/similar 端点仅有 TODO_BACKEND 草案，未进契约） | 本期新契约期望（已补记） |
| GP-8 | 覆盖检查→预览→发布 | L3 | S5-S6 | 可用（守卫/预览/发布真实，round-05 截图在案） | L3 用例 |
| GP-9 | 批改队列 URL 寻址 | L4 | S1-S2 | 可用（V2，e2e 3 用例） | L4 用例 |
| GP-10 | 建议→改分→确认下一份 | L4 | S3-S4 | 可用（乐观锁+幂等+Enter 推进） | L4 用例 |
| GP-11 | 批后讲评"最值得讲的3题" | L4→L5 | L4.S5 | **雏形在途**（讲评面板已建；mock 无 grading/insights→404 静默） | mock 补盲区 P1-7 |
| GP-12 | 学情洞察全部有动作 | L5 | S3 | **断**（kind 枚举零交集，动作全落兜底） | L5 清零（四枚举） |
| GP-13 | 课堂 Session 实时分布 | L6 | S3-S5 | **在途**（V3 已写，mock 0 覆盖） | 课堂整改（§2） |
| GP-14 | 闭环回流 54%→81% | L5→L1 | L5.S4 | **断**（效果对比数据无契约来源） | 待确认事项 #4 |
| SP-1 | 资源卡"加入这节课" | L7→L2 | L7.S4 | 待运行时复核 | L7 用例 |
| SP-2 | Butler 上下文动作 | L8 | S2 | 降级壳（后端依赖） | L8 观察项 |
| SP-3 | 空/错态不伪造 | 全链 | 各卡异常列 | V2 系达标；Legacy 系待复核 | 通用检查 |

## 6. 链路卡全集（L1-L8，逐卡确认/修订版）

> 修订记号：〔确认〕=与底稿一致；〔修订〕=对本提示词 §2 底稿的修正；每卡末尾"断点清零"即阶段 3 靶子（已全部经 §0/§2 核验）。

### L1 今日工作台 `/teacher/today`〔确认+修订〕
- **画像/场景**：A/C。打开产品第一分钟。 **入口**：登录默认落地、侧栏首项、各链路回流。
- **步骤**：S1 加载 `GET /teacher/today` 聚合 → S2 hero（下节课：班/课题/时间/备课完成度/缺项——BE 已加 6 字段）→ S3 任务清单（21 份待批→去批改深链）→ S4 洞察卡（a=0 边界 +【加入下节课】【出巩固题】【看依据】）→ S5 Butler 面板。
- **数据**：today 聚合端点（形状稳定承诺）；无本地写。
- **异常与降级**：聚合失败→hero 骨架+错误条+重试；洞察缺字段→文案模板引擎通用句式（不编造）；无课→空态引导去备课。
- **闭环**：每个任务/洞察按钮深链落点真实（批改深链迁 V2 后闭环成立）。
- **断点清零**：①:179 硬编码"17/46"→改由洞察文案模板引擎渲染后端字段；②深链取"第一份未确认"迁 V2 workspace 通路（资产表 #8）；③ButlerPanel `:open="true"` 恒展开→折叠+探测态；④T-01"同学老师"问候语运行时复核。 **GP**：GP-1、GP-14 回流端。

### L2 备课 `/teacher/prep`〔确认〕
- **画像/场景**：B（备课/出练习）、C（加入讲评）。 **入口**：侧栏、Today 洞察【加入下节课】（`?lesson_id&from=insight:{id}`，GP-2）、课堂【加入讲解】（新增，带焦点）、资源【加入这节课】（SP-1）。
- **步骤**：S1 教案列表/新建 → S2 `POST /lessons/adapt` 生成时间线 → S3 建议 diff 高亮目标环节（GP-2）→ S4 采纳 `POST /lessons/{id}/adopt-suggestion`（契约 accepted）→ S5 结构化编辑抽屉（替换 window.prompt，含 KaTeX 预览）→ S6 确认本节课（artifacts confirm，draft→confirmed 可见）→ S7 生成 PPT（slides+download，修 P-04 断链）→ S8 当堂练习（写蓝图上下文跳 Assign）→ S9 板书提纲（改后端生成）。
- **数据**：lessons/adapt、artifacts CRUD+confirm（409 乐观锁）、slides/download、adopt-suggestion、explainer；all mock 已覆盖（除 download——mock 需补，见断头路 #4）。
- **异常与降级**：LLM 降级本地模板时**明示"模板模式"徽标**（P-02 根因在后端，前端不许静默）；时长 Σ>45min 提示压缩段；slides 生成失败→错误条+重试。
- **闭环**：确认后产物按钮可用（GP-5）；当堂练习落 L3；板书提纲可下载。
- **断点清零**：window.prompt×3、采纳落库、当堂练习上下文、板书提纲后端化、:405/:421 裸 fetch 收敛。 **GP**：GP-2..GP-6。

### L3 组卷布置 `/teacher/assign`〔确认〕
- **画像/场景**：B。 **入口**：侧栏、Prep 当堂练习（带蓝图）、Classes low_mastery/error_cluster【布置练习】（带知识点）。
- **步骤**：S1 选班（记忆 context）→ S2 蓝图（知识点/题量/难度；**消费 L2/ L5 传入的预填**）→ S3 `POST /quizzes/generate` 出卷预览（题卡：选项 A/B/C/D、来源徽标、分值）→ S4 逐题换题（replace/regenerate/similar）→ S5 覆盖检查+预览学生端 → S6 `POST /assignments` + publish。
- **数据**：quizzes/generate、assignments 全套（稳定承诺）；换题三端点=新契约期望（proposed）。
- **异常与降级**：题量不足/degraded→确认发布禁用+明示原因（守卫已核验在 :102/:119）；检索质量（A-01/A-02）属后端，前端以来源徽标与"已校验"标记如实呈现。
- **闭环**：发布→学生端作答→L4 队列出现。
- **断点清零**：换题端点正式化（契约）；裸 `api.get('/classes/mine')`→classApi（:140）。 **GP**：GP-6..GP-8。

### L4 批改工作台 `/teacher/grading`〔确认，黄金路径核心〕
- **画像/场景**：A。晚自习批量批改。 **入口**：侧栏、Today 深链（`?submission_item_id`）、TeacherNav 角标、Classes review_backlog。
- **步骤**：S1 workspace 按 URL 寻址加载 → S2 左原卷（题干+学生原答/拍照件）右 AI 建议（评分点+依据）→ S3 改分/标记复看 → S4 confirm（乐观锁 version+幂等键）自动跳下一份 → S5 批后讲评面板（"最值得讲的 3 题"→加入明天讲评/变式）。
- **数据**：grading/workspace、review、confirm、file（Blob）、insights；V1 queue 仅兼容期（契约 accepted：迁移完成后由后端下线）。
- **异常与降级**：409 陈旧版本→刷新当前份；原卷文件失败→重试/复看（e2e 已盖）；insights 404→面板隐藏不阻塞。
- **闭环**：确认写入正式成绩（唯一写入口）；讲评卡→L5/L2。
- **断点清零**：V1 收敛四件套（today 深链、角标、孤儿页+契约测试、双数据通路）；mock 补 grading/insights。 **GP**：GP-9..GP-11。

### L5 班级学情→讲评 `/teacher/classes`〔确认+修订：文案模板引擎落卡〕
- **画像/场景**：C。批改完决定下节课讲什么。 **入口**：侧栏、Grading 讲评卡。
- **步骤**：S1 选班 → S2 `Promise.all(members, insights)` → S3 洞察卡（**权威四枚举**）逐卡动作：`error_cluster`→【布置变式练习】(→L3 带知识点)＋【看典型错误】(→L4)；`review_backlog`→【去批改】(→L4)；`low_mastery`→【布置针对性练习】(→L3)；`submission_trend`→【查看作业】(→L3) → S4 复盘洞察（54%→81%，GP-14，数据源待后端）。
- **数据**：classes/mine、`/classes/{id}/members`、`/teacher/classes/{id}/insights`（error_cluster 首位已 shipped）。
- **异常与降级**：insights 空→"先去批改，批完这里会长出讲评建议"引导；**洞察文案模板引擎（本期新增组件，落本卡）**：
  - 位置 `src/utils/insightCopy.ts`（命名待定）：输入=后端结构化 insight 字段，输出={title, body, evidenceRef}；
  - 规则：kind 全枚举模板；**数字一律来自字段**，缺字段→通用句式降级，禁止拼接假数字；禁 `count=`/`recent_count`/`type=rule` 上屏（红线 R2）；
  - Today 的 reason 硬编码同样改经此引擎（L1①与 L5 共用单一内核——单一内核纪律）。
- **闭环**：每个洞察动作落真实链路；无断头按钮。
- **断点清零**：mock 删 `mastery_drop/queue_pressure`、改为四权威枚举且 error_cluster 首位（裁决 c，进 L5 验收用例）；INSIGHT_ACTIONS 键适配；`:67` 死 computed、`:151-192` 死 CSS 清理；evidenceText 双份抽公共函数（N5）。 **GP**：GP-12、GP-14。

### L6 课堂 `/teacher/classroom`〔修订：Session 语义落地版〕
- **画像/场景**：A/C。课中 10 分钟检测。 **入口**：侧栏。
- **步骤**：S1 选班（fetch session：idle/active/ended）→ S2 start（课题/教室）→ S3 发题（`question_no` 0 基，已确认）→ S4 实时结果（分布/正确率/主要错误选项/AI 提醒）→ S5 变式/加入讲解（带焦点→L2）→ S6 close 归档。
- **数据**：classroom-session 四端点（后端已实现，进程内存+TTL 2h；持久化属后端阶段 3）。
- **异常与降级**：无会话→idle 启动面板；作答前分布占位→UI 标注"模拟分布"（明示，待后端诚实化字段）；断网→错误条+重试。
- **闭环**：结束回 idle；加入讲解→L2 高亮；会话数据留档供 GP-14。
- **断点清零**：§2 评审 7 项（硬编码三题/幂等/死导入/mock+e2e/裸跳转/分钟数/占位标注）。 **GP**：GP-13。

### L7 资源库 `/teacher/resources`〔确认〕
- **画像/场景**：B。 **入口**：侧栏。
- **步骤**：S1 列表/搜索 → S2 上传（multipart）→ S3 preprocess/understand → S4 publish +【加入这节课】(→L2 materials，SP-1) → S5 候选题审核 approve（真实入库）。
- **数据**：resources 全套；**mock 缺 5 端点**（external-reference/publish/unpublish/delete/approve，§0 #10）。
- **异常与降级**：上传 401→client 刷新重试（收敛后）；0 字节/MIME 校验（R-03）；摘要不可见/Markdown 不渲染（R-01/R-02）复核。
- **闭环**：资源进入备课 materials；候选题进入题库。
- **断点清零**：mock 补 5；裸 fetch 收敛（:124）。 **GP**：SP-1。

### L8 Butler 管家〔确认：维持诚实降级壳〕
- **入口**：侧栏/Today 嵌入。 **步骤**：OPTIONS 探测→404 则明示"尚未接通后端契约"；放开后 chat→（远期）SSE 流式。
- **数据**：butler/chat、capabilities。 **异常**：探测失败=诚实空态（保留模式）。
- **闭环**：动作落 artifact（SP-2 标准）。 **断点**：无前端靶（后端 PolicyGate 未放行，砍单已确认）。 **GP**：SP-2。

### 链路联网图（无断头路校验基准）
L1 为调度中心 ⇄ L2/L3/L4/L5；L2→L3（当堂练习/蓝图）；L3→L4（发布→队列）；L4→L5（讲评）；L5→L2/L3（干预回流）；L6→L2（加入讲解）；L7→L2（加入这节课）；L5→L1（复盘 GP-14）。

## 7. 断头路清单与清零方案（原 4 条 + 新增 4 条）

| # | 断头路 | 清零方案 | 归属 |
|---|---|---|---|
| 1 | config/features.js 整文件死配置 + nav.js studentNav/studentImmersive | 删除 features.js；nav.js 仅剩 adminNav→内联进 AdminNav.vue 后删文件；vitest router 契约测试护航 | P0-4 |
| 2 | 通知铃铛无 handler、红点恒亮 | 接真实最小通知源：下拉面板读 today 任务（复用 todayApi，零新契约），无未读→空态"暂无新通知"、红点熄灭 | P0-4 |
| 3 | 课堂链 mock 404（无 e2e） | mock 镜像后端测试断言（idle/active/0 基/4 项分布/ended）；新增课堂 e2e（start→launch→分布→close） | P1-7 |
| 4 | Prep 三假按钮（采纳/当堂练习/板书提纲） | 采纳→adopt-suggestion（契约 accepted）；当堂练习→蓝图上下文跳转（context store 扩展或路由 query，Assign 消费预填）；板书提纲→优先复用 `lessons/{id}/explainer` 生成端点（语义不符再走契约记录新增 board-outline） | P0-3 |
| 5（新） | L5 洞察 kind 断裂 | mock 四权威枚举（删两伪枚举）+ INSIGHT_ACTIONS 适配 + 文案模板引擎 | L5 |
| 6（新） | today 深链走 V1 通路 | 迁 V2 workspace；角标改读 V2 队列计数；V1 收敛 | L4（资产表 #8） |
| 7（新） | Prep 当堂练习/课堂"加入讲解"不带上下文（GP-6/13 断） | context store 扩展 `pendingPractice{topic,kp_codes,count,source}` 与 `focusInsight`；Assign/Prep 消费 | L2→L3、L6→L2 |
| 8（新） | lessons download 无 mock（Word 导出必挂） | teacherServer.ts 补 download 路由（返回 docx 字节流 stub） | P1-7 |

## 8. 黄金路径声明（确认维持）

**L3 → L4 → L5 作业闭环**维持为资源倾斜主轴；GP 主路径 1-14 全部挂卡对账（§5），其中 GP-3/4/6/7/12/13 六步为断点，全部已有清零方案与归属。批改 V2 为内部标杆，新组件（insightCopy、结构化编辑抽屉、通知面板）设计时引用其模式（query 同步/运行时校验适配器/幂等/乐观锁/诚实空态），并声明参照来源（纪律：One Module One Primary Reference）。

## 9. 契约期望新增（本阶段新增 2 条，已写契约记录）

1. **FE→BE**：classroom-session 题池暴露（GET session 返回 question_pool 或等价），前端删除硬编码三题——proposed。
2. **FE→BE**：`POST /teacher/quizzes/replace|regenerate`、`GET /teacher/quizzes/similar`（GP-7 逐题换题，TODO_BACKEND.md 草案正式化，出参=新 artifact 版本）——proposed。

## 10. 待用户确认事项

1. §2 课堂专项评审结论（保留为基线 + 7 项整改挂 L6）；
2. §4 双叙事合流方案六点裁决（尤其：R06 冻结存档、pages/teacher-v2 冻结、治理文档继续有效）；
3. §6 链路卡全集（尤其 L5 文案模板引擎落卡、L6 Session 卡修订）与 §7 断头路清零方案；
4. GP-14"54%→81% 效果对比"数据源本期是否落地（需后端 EffectivenessRecord 类端点，现无契约）：a) 本期向后端提案 b) 前端先做"暂无对比数据"诚实空态 c) 砍出本期；
5. 契约新增 2 条（§9）是否认可提交后端评估。
