# 阶段 4 链路验收报告（最终门禁 · 2026-09-01）

> 依据：阶段 2 用例表（59 条，`阶段2-验收用例与内容资产.md`）+ GOLDEN_PATH GP-1..14。验收环境：mock（fixture 已重置至统一数据世界）逐屏真实用户视角走查；真后端腿受阻塞（见 §4）。

## 1. 黄金路径 GP-1..14 走查结果

| GP | 步骤 | 结论 | 证据 | 备注 |
|---|---|---|---|---|
| GP-1 | 登录→Today | **PASS** | `evidence/GP-01-today.png` | hero(备课 70%)/4 任务/洞察三动作/李老师；无 count=/recent_count/confidence/type=rule（精确正则复核） |
| GP-2 | 洞察→备课 diff | **PASS（修复后）** | `GP-02-insight-to-prep.png` | **走查抓到缺口**：Today【加入下节课】丢失 insight 上下文→当轮修复（携带 ?lesson_id&from=insight:）；建议卡+依据 17/46 渲染；插入点高亮需已有教案（无教案时诚实降级=仅建议+依据） |
| GP-3 | 采纳→时间线变化 | **PASS** | `GP-03-04-edit.png` | 采纳→落库（adopt-suggestion 幂等键）→环节 2 标"已采纳" |
| GP-4 | 结构化编辑 | **PASS** | 同上 | 抽屉打开/编辑保存/公式保留；**遗留 P3**：时间线正文为纯文本，LaTeX 源码直显（预览抽屉内已渲染） |
| GP-5 | 确认本节课 | **PASS** | `GP-05-confirm.png` | toast"本节课已确认，可生成正式 PPT"；产物按钮解锁 |
| GP-6 | 当堂练习蓝图 | **PASS** | `GP-06-07-assign.png` | URL 带 count=3/source=lesson:/topic/kp_codes；组卷台全表单预填；3 题渲染（KaTeX/来源"本地样例"） |
| GP-7 | 逐题换题 | **PASS（降级口径）** | 同上 | 端点未 shipped→换一题禁用+明示"已提请后端契约"；shipped 后按 TC-L3-F04 转正常断言 |
| GP-8 | 覆盖→预览→发布 | **PASS** | `GP-08-publish.png` | 守卫/确认/创建/两步发布全通；"作业已发布，目标班级的学生端现在可见" |
| GP-9 | 批改队列寻址 | **PASS** | `GP-09-11-grading-loop.png`（另见 L4 证据） | Today 深链→`?submission_item_id=si-1`（V2 workspace 通路，V1 调用=0）；21 份队列 |
| GP-10 | 建议→改分→确认下一份 | **PASS** | 同上 | 改分 9→确认→自动切 si-2；"已切换至下一份" |
| GP-11 | 批后讲评 | **PASS** | 同上 | "本次作业最值得讲的"+17 人错+加入明天讲评（insights 真实端点） |
| GP-12 | 学情→干预 | **PASS** | `L5-insights-four-kinds-actions.png` | 四枚举洞察全渲染；干预按钮落点真实（布置变式练习→assign?kp_codes=函数的单调性） |
| GP-13 | 课堂 Session | **PASS** | `L6-session-live.png` | 开课→发题（0 基）→实时分布 66%→AI 提醒→"分布为演示数据"诚实标注 |
| GP-14 | 闭环回流 54%→81% | **FAIL（遗留）** | — | 后端无效果对比数据源（EffectivenessRecord 无契约）；前端诚实空态 UI 亦未落（阶段 3 遗留声明项）。**不阻塞验收，列为下期第一优先** |
| SP-1 | 资源→备课 | **部分** | `L7-resources-actions.png` | mock 空资源→诚实空态；"加入这节课"CTA 未实现（遗留 P2） |
| SP-2 | Butler 上下文 | **PASS（降级壳）** | L1 证据 | 探测失败明示未接通（后端依赖，砍单项） |
| SP-3 | 空/错态不伪造 | **PASS** | L7 证据 | 空态均有原因+下一步 |

**走查结论：14 步中 12 PASS（含 1 修复后过、1 降级口径过）、1 FAIL（GP-14，遗留）、1 部分（SP-1）。**

## 2. 用例表（59 条）状态汇总

| 链路 | 通过 | 降级通过 | 部分/未实现 | 延后/待真后端 |
|---|---|---|---|---|
| L1（10） | 9 | — | — | E01 视觉对照△（截图在案） |
| L2（12） | 9 | — | — | F07 下载为 mock stub（真后端复验）、E01△、**E02 延后**（ai-baseline 未就位，interim 结构对比见 §3） |
| L3（8） | 7 | F04（诚实降级） | — | E01△ |
| L4（10） | 10 | — | — | F04 真后端模式复验待账号 |
| L5（8） | 6 | — | **F04 部分**（名册✓/7 人名单过滤未做）、**F06 未实现**（GP-14） | — |
| L6（9） | 9 | — | — | — |
| L7（6） | 3 | — | **F05 未实现**（"加入这节课"CTA） | F02/F04 待真后端 |
| L8（3） | 2 | — | — | E01△ |
| **合计 59** | **通过 46 + 降级 1 ≈ 79%** | | 未实现 3 + 部分 1 | 待真后端 4、延后 1、视觉△5 |

**用例通过率：46/59 显性通过 + 1 项按裁决降级通过 = 79.7%；无"假通过"——3 项未实现全部如实标 FAIL/部分并进遗留清单。**

## 3. AI 生成物对比验收表（interim 基准）

> ai-baseline 基准库未就位（后端建设项），按阶段 2 裁决以 `research-repos/teacher-ground-truth/`（人教社/学科网/讯飞真实教研资料结构清单）为 interim 结构基准。

| 对比项 | 基准要求（ground-truth） | 我方产物（mock adapt，GP-03 截图） | 判定 |
|---|---|---|---|
| 环节结构 | 环节 ×（教师活动\|学生活动\|分钟\|设计意图\|教学评价） | segment 含 title/duration_min/kind/teacher_action/student_action/core_question/content/assessment_check/learning_objective/source/locked —— **基准五要素全覆盖+4 个扩展字段** | ✅ 结构完整 |
| 内容密度 | 每环节有具体提问/活动，非通用模板 | "提问 y=x² 的单调性…再抛出三次函数定义法繁琐"等真实教研话术（来源 teacher-ground-truth 语料） | ✅ |
| 课时约束 | Σ≤45min 且可重平衡 | 6 环节 45min + 自动平衡工具 | ✅ |
| 排版 | 文档式教研表格 | 工作台时间线卡片（工作台场景适配） | ⚠️ 形态差异（场景合理），文档化排版待 ai-baseline 就位后并排对比 |
| 采纳可溯 | 建议插入有依据 | 建议卡带"依据：17/46…"，采纳写 source=teacher_edit/已采纳标记 | ✅ |

**结论：结构维度达到 interim 基准；排版并排对比在 ai-baseline 就位后执行（TC-L2-E02 已挂延后）。**

## 4. 真后端腿阻塞申报（红线 5 如实记录）

- 真后端服务**存活**：`http://127.0.0.1:8000/api/health` → `{"status":"ok","version":"1.0.0"}`。
- **阻塞点**：无可用真实教师账号——注册走短信挑战（`/register/sms` 需 challenge_id+code）、库内无公开种子账号、R06 指令"后端 D:\math-arena 禁改"使我方不可自建账号/数据。
- **影响用例**：TC-L4-F04（真后端 confirm 持久化复验）、TC-L7-F02/F04（真后端上传/审核）、以及黄金路径真后端录屏。
- **解除条件**（二选一）：a) 用户提供测试教师账号（手机号+密码）；b) 后端对话 seed 一个验收账号并将凭据写入契约记录。

## 5. 遗留问题清单（含严重级别）

| 级别 | 项 | 去向 |
|---|---|---|
| P1 | GP-14 效果对比：后端 EffectivenessRecord 无契约 + 前端诚实空态未落 UI | 下期第一优先；需 FE→BE 契约提案 |
| P1 | 三条 proposed 契约（换题三端点/采纳落库后端实现/题池暴露）shipped 后回归 TC-L3-F04、TC-L2-F04、TC-L6-F03 | 契约记录跟踪 |
| P2 | "加入这节课"CTA（SP-1）、7 人名单过滤（TC-L5-F04 后半）、时间线 LaTeX 渲染（GP-4 瑕疵） | 下期 P2 批次 |
| P2 | 真后端腿 4 用例（待账号） | 账号解除后补走 |
| P3 | teacher.css 双轨收敛（P2-9）、Classes 名册窄屏、仓库卫生（P2-10） | 有余量才做 |
| P3 | artifacts/teacher-v2 下 4 张 e2e 截图被验收跑重渲染（未提交的工作区噪音） | 下次 e2e 跑后一并处理 |

## 6. 证据索引

- GP 走查截图：`docs/teacher-optimize/evidence/GP-01~GP-08-*.png`（GP-09-11 截图同屏证据见 `L4-deeplink-V2-review-panel.png`；GP-12/13 见 `L5-*`/`L6-*`）
- 阶段 3 全量证据：`docs/teacher-optimize/evidence/L1~L7-*.png`
- 自动化：vitest 34 文件/213 用例全绿；e2e 34/34 全绿（`npx playwright test`，workers=1）；typecheck 仅 2 处已知学生侧错误
- 提交链：`f5e9b5a…0982d07`（8 个链路提交）+ 本报告提交

## 7. 验收判定建议

**前端侧：具备验收通过条件**（79.7% 显性通过 + 1 项裁决降级 + 全部未实现项如实入册且不阻塞黄金路径主链）；**两项待办出 finals**：①真后端腿（待账号）②GP-14/三条契约 shipped 后的回归补课。请用户裁定：a) 按现状通过阶段 4、真后端腿与 P1 遗留转下期；b) 先解除账号阻塞补走真后端腿再出验收结论。
